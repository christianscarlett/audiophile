
class AudioService {

  constructor(audioElement, gainControl, playButton) {
    this.audioElement = audioElement;
    this.gainControl = gainControl;
    this.playButton = playButton;
  }

  

}

export default AudioService;